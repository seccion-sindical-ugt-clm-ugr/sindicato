# ====================================
# CONFIGURACIÓN DE ENTORNO - UGT-CLM-UGR
# ====================================
# Este archivo es una plantilla. Copia este archivo como .env y rellena los valores.
# NUNCA commitees el archivo .env al repositorio (está en .gitignore)

# ====================================
# ENTORNO
# ====================================
NODE_ENV=development
# Valores: development | production

# ====================================
# SERVIDOR
# ====================================
PORT=3000

# ====================================
# BASE DE DATOS - MongoDB Atlas
# ====================================
# Obtén tu URI desde: https://cloud.mongodb.com
# Formato: mongodb+srv://usuario:password@cluster.mongodb.net/dbname
MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/ugt_sindical

# ====================================
# SEGURIDAD - JWT
# ====================================
# Token secret para autenticación de usuarios
# Genera uno nuevo con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_generate_with_crypto

# Duración de tokens (opcional, defaults: 7d y 30d)
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# ====================================
# SEGURIDAD - ADMINISTRACIÓN
# ====================================
# Contraseña para el panel de administración
# Genera una con: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ADMIN_PASSWORD=your_admin_password_here_generate_with_crypto

# ====================================
# STRIPE - Pagos
# ====================================
# Obtén tus claves desde: https://dashboard.stripe.com/apikeys

# Clave secreta (NUNCA la expongas públicamente)
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxx

# Clave pública (se puede exponer en el frontend)
STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxx

# Webhook secret para verificar eventos de Stripe
# Obtén desde: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxx

# URLs de redirección después del pago
SUCCESS_URL=https://tu-dominio.com/success.html
CANCEL_URL=https://tu-dominio.com/cancel.html

# ====================================
# CORS - Seguridad de Orígenes
# ====================================
# Lista separada por comas de dominios permitidos para hacer requests al backend
# Ejemplo: https://dominio1.com,https://dominio2.com
ALLOWED_ORIGINS=http://localhost:8000,http://localhost:3000,https://ugtclmgranada.org

# ====================================
# EMAIL - Notificaciones (Opcional)
# ====================================
# Si no están configurados, los emails no se enviarán (el sistema seguirá funcionando)

# SMTP Server
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# Credenciales de email
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu-password-de-aplicacion

# Email remitente
EMAIL_FROM=noreply@ugtclmgranada.org
EMAIL_FROM_NAME=UGT CLM Granada

# Email para recibir notificaciones de admin
ADMIN_EMAIL=admin@ugtclmgranada.org

# ====================================
# NOTAS IMPORTANTES
# ====================================
# 1. Genera secrets aleatorios y únicos para JWT_SECRET y ADMIN_PASSWORD
# 2. NUNCA uses las claves de test de Stripe en producción
# 3. Asegúrate de configurar ALLOWED_ORIGINS correctamente en producción
# 4. Configura MongoDB Atlas whitelist (IP: 0.0.0.0/0 para Vercel)
# 5. En Vercel, configura estas variables en: Settings → Environment Variables
