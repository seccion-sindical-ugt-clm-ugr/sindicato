# ====================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# UGT-CLM-UGR Backend API
# ====================================

# Puerto del servidor
PORT=3000

# Entorno (development, production)
NODE_ENV=development

# ====================================
# STRIPE - Pagos
# ====================================
# Clave secreta de Stripe (obligatoria)
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# URL de tu dominio para redirecciones
DOMAIN=http://localhost:8000

# ====================================
# MONGODB - Base de Datos (Opcional)
# ====================================
# URI de conexión a MongoDB (para sistema de sugerencias)
# Ejemplo: mongodb+srv://username:password@cluster.mongodb.net/ugt-database
MONGODB_URI=

# ====================================
# EMAIL - Notificaciones (Opcional)
# ====================================
# Configuración SMTP para enviar emails
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=tu-email@gmail.com
EMAIL_PASS=tu-contraseña-de-aplicacion

# Email del administrador para recibir notificaciones
ADMIN_EMAIL=admin@ugtclm.org

# ====================================
# SEGURIDAD
# ====================================
# Contraseña para acceso al panel de administración
ADMIN_PASSWORD=ugt2024admin

# JWT - Autenticación (OBLIGATORIO)
# Secreto para firmar tokens JWT - ¡Cambia esto en producción!
# Genera uno aleatorio con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=super-secreto-cambiar-en-produccion-ugt-clm-ugr-2024
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# Orígenes permitidos para CORS (separados por comas)
ALLOWED_ORIGINS=http://localhost:8000,http://localhost:3000,https://elcorreveidile.github.io

# ====================================
# CONFIGURACIÓN ADICIONAL
# ====================================
# Nivel de logs (error, warn, info, debug)
LOG_LEVEL=info

# Tiempo de sesión en minutos
SESSION_TIMEOUT=30

# ====================================
# INSTRUCCIONES
# ====================================
# 1. Copia este archivo a .env
# 2. Configura las variables necesarias
# 3. No subas el archivo .env al repositorio (ya está en .gitignore)
# 4. Para producción, usa variables de entorno del hosting
