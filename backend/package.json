{
  "name": "ugt-clm-ugr-backend",
  "version": "1.0.0",
  "description": "Backend API para gestión de pagos y afiliaciones UGT-CLM-UGR Granada",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "echo \"No tests yet\" && exit 0",
    "verify-env": "node scripts/verify-env.js",
    "generate-jwt-secret": "node -e \"console.log('\\n🔑 Tu nuevo JWT_SECRET:\\n'); console.log(require('crypto').randomBytes(64).toString('hex')); console.log('\\n⚠️  Cópialo a tu archivo .env como JWT_SECRET=...\\n')\"",
    "test:db": "node -e \"require('dotenv').config(); const mongoose = require('mongoose'); mongoose.connect(process.env.MONGODB_URI).then(() => { console.log('✅ MongoDB conectado correctamente'); process.exit(0); }).catch(e => { console.log('❌ Error conectando a MongoDB:', e.message); process.exit(1); })\"",
    "test:stripe": "node -e \"require('dotenv').config(); const stripe = require('stripe')(process.env.STRIPE_SECRET_KEY); stripe.balance.retrieve().then(b => { console.log('✅ Stripe conectado -', b.available[0]?.amount / 100 || 0, b.available[0]?.currency || 'EUR'); process.exit(0); }).catch(e => { console.log('❌ Error Stripe:', e.message); process.exit(1); })\""
  },
  "keywords": [
    "stripe",
    "payments",
    "express",
    "api"
  ],
  "author": "UGT-CLM-UGR Granada",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "blob-stream": "^0.1.3",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-mongo-sanitize": "^2.2.0",
    "express-rate-limit": "^7.1.5",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "jspdf": "^3.0.3",
    "mongoose": "^8.0.3",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.7",
    "pdfkit": "^0.17.2",
    "stripe": "^14.10.0",
    "xss": "^1.0.14"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
